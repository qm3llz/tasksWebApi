info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/login
  body:
    type: json
    data: |-
      {
        "username": "user",
        "password": "pass"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
