info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/register
  body:
    type: json
    data: |-
      {
        "username": "user",
        "password": "pass"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
